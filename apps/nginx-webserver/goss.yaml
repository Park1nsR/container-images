---
process:
  nginx:
    running: true

port:
  tcp:80:
    listening: true
  tcp:443:
    listening: true

http:
  http://localhost:80:
    status: 200
    body:
      - '<title>Nginx Webserver</title>'
